# Literature Multiverse post-hoc moderator proposal

Prompt version: `moderator-proposal-v1`

The pre-specified headline analysis is frozen. Review the supplied residual opposite-direction
pairs and the complete existing moderator table. Propose at most one additional measurable context
field that is not already present. Return exactly the supplied strict JSON schema.

The proposal must define a stable slug, display name, value type, exhaustive categories or numeric
bins, extraction instruction, missing-value behavior, whether it is paper-constant or within-paper,
and a valid paper-level exchangeability rule. It must be recoverable from the already archived
paper text for the whole eligible v1 cohort. Do not claim it explains the disagreement; it is a
post-hoc candidate awaiting human approval and incremental testing.

Existing moderators:

`[[MODERATOR_TABLE_JSON]]`

Residual pairs:

`[[RESIDUAL_PAIRS_JSON]]`
