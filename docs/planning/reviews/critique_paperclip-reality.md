VERDICT: The design is unusually honest about its biggest unknown (D2 array schema) but its smoke test validates only the happy path: 3 known-good papers, one map call, presence-not-correctness checks. Every stage that consumes Paperclip *after* 5 PM depends on behaviors s0 never touches — windowing determinism over screened result sets, zero-finding row semantics, CLI --json shape and error taxonomy, evidence-line accuracy and citation-URL resolution, and the joint lifetime of result IDs, OAuth tokens, and rate limits across an unattended overnight run with no resume design. The two structural gaps: (1) the overnight M5 plan has three independent unverified ways to die at 3 AM and 'save raw JSON locally' does not make extraction resumable (blocker); (2) the D2 fallback chain is thinner than it looks — pass 1's 'array of strings confirmed-safe' is not in the verified list, SDK-execute is the same unverified schema path as the thing it backs up, and the Claude-API rung depends on an Anthropic key whose availability is still an open question. On the explicit D1 question: subprocess CLI is the correct primary (it is the only verified schema path), but only if specified as argv-list-no-shell with an s0-pinned --json parse contract and a forced-error probe; shell quoting of large schemas is a non-issue with argv lists and a near-certain 10 PM bug without them. Highest-leverage change: expand s0 from 'does the array schema validate' to 'does the full extraction path behave' — API-key auth (not OAuth session), two-window union check over ~20 papers including one review, quote-in-cited-lines verification plus one clicked citation URL, one forced error, one filter timing — roughly 45 extra minutes that convert most majors into verified facts before code exists, and gate G1 on that expanded probe. Files reviewed: /Users/harry/Desktop/temp/reAgentsHack/docs/superpowers/specs/2026-08-15-literature-multiverse-design.md, /Users/harry/Desktop/temp/reAgentsHack/docs/planning/research-notes.md, /Users/harry/Desktop/temp/reAgentsHack/LITERATURE_MULTIVERSE_PROJECT_CONTEXT.md.

[blocker] (M5 overnight scale-up (§6) + D3 + s0 step 1 (§5.1)) The unattended overnight run (scale corpus, remap, 200x permutation, 500x bootstrap) stacks three unverified Paperclip dependencies simultaneously — search-result-ID TTL (research-notes unknown #6), auth-session lifetime (user is logging in via browser OAuth right now; token TTL never checked, and s0 step 1 only does `config`/whoami, which passes on the OAuth session), and rate limits (unknown #3) — with no resume/checkpoint design. The stated mitigation ('save all raw JSON locally; local persistence is the source of truth') covers reproducibility of completed windows, not resumability: `map` needs a live server-side result ID, and a fresh search is not guaranteed to reproduce ordering, so `--offset` arithmetic breaks against a re-created result ID.
FAILURE: At ~2–3 AM the OAuth token expires or the s_ ID is gone; tenacity retries a permanent auth/not-found error until backoff exhausts; solo hacker is asleep; discovered ~7 AM Sunday with 40% of the corpus and no time for the permutation/bootstrap runs that are the demo's headline credibility stats. Demo falls back to the 30-paper v1 dataset and the 'scaled multiverse' story quietly becomes a 30-paper story.
FIX: (a) In s0, mint a durable API key at paperclip.gxl.ai/keys and run the map probe with PAPERCLIP_API_KEY explicitly (OAuth session removed/ignored) — overnight runs must never ride the browser-login token. (b) Promote result-ID TTL and rate limits from 'open questions' to G1-blocking questions to organizers today. (c) Build s3 checkpointed per-window with paper_id-keyed dedupe on merge, plus a re-search fallback that reconciles by paper_id set-difference, never by offset alignment. (d) Schedule the full corpus run to finish before the 11 PM day close while awake; treat overnight as buffer, not plan.

[major] (D3 windowed maps + s0 step 4 (§5.1)) s0 probes the array schema on 3 hand-picked papers in one call, but never probes the windowing mechanics every later stage depends on: (i) whether --limit/--offset ordering is stable and non-overlapping across separate map invocations; (ii) whether result IDs produced by the s2 screening chain (refine → merge → filter --from) are themselves mappable with windows — D3 says windows run against 'one search result ID', but extraction must run on the *screened* set; (iii) map behavior on a zero-findings paper (research-notes unknown #5: null-fill vs silent row-drop) — the D4 screening design *requires* reviews to come back as eligible=false rows, not vanish; (iv) failure granularity when the single schema-correction retry fails: per paper or per 10-paper window. Even s0's own step 4 silently assumes 3 specific anchor papers can be assembled into a mappable result ID (via lookup? merge?), which is itself unverified.
FAILURE: Overnight windows silently skip or duplicate papers, or no-finding papers are dropped instead of returned — corpus denominators ('N of M eligible'), entropy, and per-paper grouping are computed on a silently corrupted corpus, and nobody notices until a judge (the room includes the Garden-of-Forking-Paths coauthor) asks why the paper counts don't reconcile. Or: one malformed paper makes its whole window fail after the correction retry, and tenacity re-runs the doomed window all night.
FIX: Extend s0 by ~10 min: run a ~20-paper search, map it as two windows (--limit 10 --offset 0 and 10), assert union == search list with zero overlap; deliberately include one review and one likely-irrelevant paper in the probe set to observe zero-finding behavior; run one map against a filter-derived result ID; spec bisect-to-`--limit 1` on window failure in paperclip_client instead of fixed-window retry.

[major] (D8 provenance + schema §4.2 + s0 step 4) The 'never cut' provenance feature assumes three things s0 checks for presence but not correctness: (i) evidence_lines come back as parseable line numbers — the schema types them as array-of-strings, which could equally be 'Results, para 3'; (ii) they are *accurate* (the quote actually lives at those lines in content.lines); (iii) the constructed citation URL resolves with the #L<n> anchor for that doc_id. Worse: the extraction schema has the *model* fill paper_id, but D8's URL needs the authoritative doc_id from the map result envelope — and whether `map --json` attaches a per-item source doc ID is unknown (output shape unverified). Trusting a model-filled ID puts the most hallucination-prone field at the root of the provenance chain.
FAILURE: Sunday ~6 AM dashboard assembly discovers evidence_lines are section labels or off-by-preprocessing line numbers; citation links 404 or highlight the wrong sentence; during live judging someone clicks an evidence card and 'every claim traceable to source lines' — the one-sentence pitch — visibly fails on the exact feature that differentiates the project.
FIX: Add to s0 step 4: for each probe paper, pull content.lines and assert evidence_quote is a substring of the cited line range; pin the evidence_lines format observed (integers vs 'Lx-Ly') in the schema description and the parser; construct one real citation URL and open it in a browser today; confirm map --json carries an authoritative per-item doc id and join extraction rows to *that*, never to the model-filled paper_id.

[major] (D1 subprocess-CLI choice) D1 is the right primary — the CLI --output-schema flag is the only *verified* schema path — but it is underspecified exactly where subprocess integrations die, and its Fallback 1 is largely illusory. Unspecified: `map --json` stdout shape (envelope vs NDJSON vs progress noise mixed into stdout); CLI error taxonomy (exit codes; how rate-limit vs schema-rejection vs paper-fetch failures present), without which the planned tenacity policy cannot distinguish retryable from permanent errors; CLI-side timeout on a heavy 10-paper array-schema window (SDK's is 300 s; 10 papers × 30 s+ flirts with it, and CLI behavior is unknown). Shell quoting is only a non-issue if paperclip_client is specified as argv-list subprocess without shell=True — the design never says so, and an AI coding agent's default f-string shell command breaks on the first apostrophe inside a schema description. Meanwhile Fallback 1 (SDK `execute("map", ...)`) is precisely the unverified SDK-schema path (research-notes unknown #2) hitting the same backend — not an independent fallback. The real chain is CLI → Claude-API.
FAILURE: First error at scale (rate limit at 1 AM) is retried identically to a permanent schema rejection — or a permanent error burns 8 exponential-backoff retries per window before surfacing; or a 10-paper window times out client-side while the server-side map completes, spending quota with nothing archived; or the agent-generated shell string dies cryptically on an embedded quote at 10 PM while you're validating dataset v1.
FIX: Spec paperclip_client now: argv-list subprocess only (no shell); probe in s0 whether --output-schema accepts a file path, else pass schema as one argv element; s0 archives one raw `map --json` stdout and pins the parser to it; s0 forces two failures (bogus result ID; deliberately invalid schema) and records exit codes + stderr shape; retry-allowlist keyed to the observed taxonomy; window size capped so measured per-paper latency × window < min(observed timeout, 300 s). Demote SDK-execute to 'try for 10 minutes at G1, else strike from the doc'.

[major] (D2 fallback chain + D7 + open question 3 (§10)) If G1 fails, the design says 'pick the D2 fallback NOW' — but both fallback rungs are themselves unverified, and the doc miscredits one as safe. The two-pass fallback's pass 1 is annotated 'array of short strings confirmed-safe', which appears nowhere in research-notes' confirmed list (the docs example is a flat object; if map rejects array-typed properties at all, it plausibly rejects this too). D1-F2 (Claude-API extraction over pulled content.lines) has unexamined prerequisites and silent losses: it needs an ANTHROPIC_API_KEY whose distribution mechanism is UNVERIFIED ('credits at event') and is still an open question to the user; pull/cat rate limits for 50–120 full texts are unknown; it loses map_result_id — a D8 provenance field on every row — and Paperclip's server-side map parallelism; and the two-pass variant multiplies map calls by #comparisons per paper, a time cost the plan never budgets.
FAILURE: G1 fails at ~3 PM; the two-pass fallback fails the same way 20 minutes later; pivot to Claude-API extraction stalls because no working Anthropic key exists until event credits are sorted — extraction architecture is still unresolved in Build Session II, the ≤5 PM topic-lock gate slips, and every downstream milestone compresses.
FIX: Probe the fallback inside the same s0 session: one extra map call with an array-of-strings schema validates pass 1's premise for one call's cost. Verify a working Anthropic key *today* (it is already open Q3 — tie it to the gate: G1 is not 'passed' until either the array schema works or one fallback path is tested end-to-end). Pre-write content.lines line-number bookkeeping so the Claude path preserves evidence_lines, and record fallback rows with map_result_id=null plus an extraction_engine field so provenance degradation is explicit rather than silent.

[minor] (s0 step 2 count queries + G2 triage (§5.2)) Topic lock leans on `-c` count queries, but `-c` semantics are uncalibrated: total index matches vs capped/retrievable count, and interaction with --source and -e are unknown. Against a 150M-abstract index, raw match counts can exceed 'usable eligible primaries' by orders of magnitude, and differently per topic phrasing — cross-topic comparisons become noise.
FAILURE: Topic C shows 800 matches vs A's 2,100 driven by synonym breadth, not corpus reality; topic locked at 5 PM on incomparable numbers; the true eligible-primary count surfaces at M3 when C yields ~25 usable primaries (below the ~60 threshold research-notes sets for C) with no time left to switch.
FIX: Calibrate once in s0: same query with -c and with -n 100 --json plus a quick screen-rate eyeball; base G2 primarily on the 10-paper probe extraction yield (already planned) and demote -c to tie-breaker.

[minor] (s0 step 3 anchor lookups (§5.1) + G3) Anchor presence/full-text checks ride entirely on `lookup doi|pmid|title` reliability, which is unverified. A lookup false negative (indexing gap, DOI variant, title fuzz) is indistinguishable from 'paper absent', and anchors gate two decisions: topic scoring (full-text-rich?) at G2 and the anchor-match extraction sanity check at G3.
FAILURE: Paulsen 2014 / Yfanti lookups return nothing due to an ID-resolution quirk; topic C is down-scored as 'abstract-only corpus' and the 5 PM lock swings to A on an artifact — or G3's anchor-extraction check simply can't run, weakening the validation story.
FIX: For every anchor miss, cross-check with an exact-phrase title search (-e) and a corpus grep for a distinctive string before declaring absence; record present/absent-with-method in docs/planning/triage.md.

[minor] (D4 screening + 90-min triage budget (§5.2)) D4's cheap-first ordering assumes --article-type carves off most reviews, but it is PMC-only (design annotates this) — what it misses is the consequence: for biorxiv/medrxiv/abstract-heavy slices the LLM `filter` becomes the primary screen, and filter latency plus its per-paper caching semantics (cache keyed on paper × claim wording?) are unprobed by s0, while triage must screen 3–4 topics × multiple query families inside 90 minutes, with every claim rewording potentially busting the cache.
FAILURE: filter runs at a few seconds per paper across a few hundred papers per topic; triage burns its budget on screening waits and G2 slips past 5 PM — or screening is skipped for speed and review pollution (worst for topic A per research-notes) contaminates the probe entropy scores G2 ranks on.
FIX: Time one filter call over ~20 papers in s0; if slow, reorder triage to score on refine/sql metadata plus the 10-paper probe first and run the LLM filter only on the winning topic; freeze filter claim wording per topic before triage starts to exploit the cache.

