VERDICT: Borderline top-3 as designed; the science layer is unusually well-armored, but the demo layer is the under-specified half — and the demo is what's scored. Of the seven narrative beats in the 90-second script, four currently have no producing artifact or no rendering plan (baseline comparison, legible tree, agent-loop trace, the number-bearing takeaway sentence), and two of the strongest beats have no fallback if reality declines to cooperate (no-pattern outcome at M4, discarded agent proposal). The differentiation claim as scripted ('we discover the moderators') is falsified by the project's own config file and will not survive one skeptical judge question, even though the survivable answer already exists in the research notes. The fixes are cheap NOW — roughly: a demo-artifact contract (one frozen JSON per narrative beat, emitted by the pipeline, including trace.json and baseline.json), a pre-committed headline-sentence template whose every number has a named statistic behind it, a tree-renderer treated as its own deliverable tonight, and two pre-scripted narrative variants (regime-found, disagreement-is-the-finding). With those, this scores top-3 on the strength of provenance + honest statistics + a demonstrably real agent loop, which no adjacent tool (AutoSynthesis, Elicit, Consensus) can match on stage. Without them, it demos as a jargon table, an sklearn screenshot, and a differentiation claim that dies in Q&A — a pass.

[blocker] (Design §6 M4 'pattern check' + §9 demo narrative) The M4 gate ('is there a pattern?', ≤10:30 PM Sat) has no failure branch, and the 90-second narrative has no variant for the no-pattern outcome. Topic locks at G2 (5 PM); pattern absence is discovered 5.5 hours later with no un-lock protocol, no scripted fallback story. Beats 30–82s of the narrative (tree splits, regimes appear, agent gain) all presuppose a discovered regime. The context file (§22.6) contains the honest fallback framing — 'residual disagreement is itself the finding, plus the map of where evidence is missing' — but the design never turns it into a demoable narrative.
FAILURE: 10:30 PM Saturday: entropy is high (0.85) but no moderator survives the grouped permutation control on ~30 papers (entirely plausible at that n — grouped CV with k=5 on 30 papers is noisy). You now face a choice between re-triaging a topic overnight with rate-limit-bounded extraction, or presenting a demo whose centerpiece tree has one uninformative split. Either way the decision is made exhausted at 2 AM instead of calmly at 2 PM today. Judges see a tree that doesn't separate anything and the pitch line 'we tell you when the answer changes' is directly falsified on screen.
FIX: Write BOTH narrative variants now, before any code: (A) regime-found script as drafted; (B) 'the disagreement is real and unexplained' script — headline becomes the residual-contradiction pairs + evidence-desert map ('after conditioning on everything papers report, these 9 study pairs remain incompatible — here is the experiment that would resolve them'). Add to M4 an explicit decision rule: if no moderator passes permutation p<0.1 by 11 PM, switch the build to variant B (contradiction cards + coverage map get the polish hours, tree gets demoted to one slide). Also pre-authorize the one legal pivot: the C→C+B merge config is already prepared — make 'merge and rerun s5 only' the sole permitted overnight topic change.

[blocker] (Design §9 narrative closer + D6 metric choice) There is no committed takeaway sentence with a number anywhere in the design, and the plan's own metric choices actively prevent one. The pre-registered headline statistic is grouped-CV Δlog-loss — a quantity with no intuitive translation ('log loss improved by 0.14' means nothing in 90 seconds). The intuitive statistic ('X% of directional disagreement explained', normalized MI, context §13.2) is explicitly demoted in D6 to 'descriptive companion only'. The narrative says 'held-out gain +X%' — a percentage of WHAT is undefined. Meanwhile the closer ('Literature agents tell you what papers say...') contains zero numbers. Judges seeing 20+ projects remember exactly one sentence per project; this plan doesn't produce one.
FAILURE: Sunday 12:45 PM: the presenter improvises '...explains 31% of the disagreement' from the MI column because it sounds better than the log-loss number. A stats-literate judge (James Zou co-hosts; forking-paths is his paper) asks 'which statistic is that 31%?' — and the honest answer is 'the one we pre-registered as unreliable at this sample size.' The credibility layer, the project's best asset, self-detonates. Alternative failure: presenter says 'held-out log loss improved 0.14' and the number evaporates from every judge's memory within one project.
FIX: Pre-commit NOW to one sentence template with defined slots and make s5 compute exactly its ingredients: e.g. '[N] papers, [M] findings, globally 45/30/25 positive/null/negative — but inside the [moderator] regimes, [P]% of findings agree with their regime's majority direction (vs [Q]% globally), and that structure holds in [B]% of paper-level bootstraps.' Within-leaf majority agreement is honest, judge-parsable, and derivable from the already-planned tree + bootstrap artifacts. Have s5 emit a headline.json containing the filled sentence so the dashboard header and the spoken script cannot drift apart. Decide today the exact answer to 'which statistic is that?' for every number in the sentence.

[major] (Design D9 / scope item 5 — the multiverse tree) The tree is the wow moment (beats 30–55s) and it is the only screen with no rendering plan. sklearn on one-hot moderators natively yields node labels like 'species_Mus musculus <= 0.5, gini=0.43' — unreadable to judges at projector distance. Plotly has no decision-tree chart type; a legible custom tree (or the ASCII-mockup style in context §14 with readable split labels, direction-colored leaves, and n_papers per leaf) is several hours of bespoke layout work that appears nowhere in the milestones — it's implicitly buried in 'dashboard assembled' Sunday ≤9 AM, the highest-pressure slot of the event.
FAILURE: Sunday 7 AM: analysis artifacts are done, and the agent produces the fastest thing that renders — matplotlib plot_tree screenshot embedded in Streamlit. The centerpiece of the demo is a blurry sklearn diagnostic plot with feature-engineering jargon in every node. The 15 seconds that were supposed to produce the wow instead produce squinting. Noticed exactly when there is zero time to build the custom visual.
FIX: Treat the tree renderer as a standalone deliverable with its own milestone slot TONIGHT (it needs only a fitted tree + label map, both available after M4). Spec it in one paragraph now: human-readable split labels from a moderator→display-name map ('vitamin C dose ≥ 1000 mg/day', not 'dose_ge_1000 <= 0.5'), leaves as stacked direction bars with 'n=12 findings / 7 papers' captions, max depth 3 so it fits one screen. A hand-built Plotly shapes/annotations tree or even a styled Graphviz export is fine — but it must exist and be reviewed against a projector-legibility check before Sunday morning.

[major] (Scope item 7 + D7 — agent-loop trace) The trace is promised on screen ('Trace displayed in demo') but no pipeline stage produces a trace artifact. s5 emits 'entropy, moderator table, tree, pairs, boots'; s6 emits raw remap JSON. Nothing emits the structured record the screen needs: proposal JSON + input contradiction pairs + human approval + remap result-ID + held-out ΔLL before/after + keep/discard decision + timestamps. Separately, there is no narrative fallback if the single budgeted iteration's proposal FAILS the held-out gain test — with a timing-type moderator at typical ~40-60% coverage on a small corpus, a null or negative gain is a coin-flip-likely outcome, and M5 (overnight) leaves no time for iteration two.
FAILURE: Two ways this dies. (a) 2 AM Sunday: the trace section gets hand-assembled from scrollback into static text — at an 'end-to-end agentic science' event, a judge asks 'can I see the actual proposal output?' and the answer is copy-paste archaeology; the trace reads as staged, which taints the whole provenance story. (b) 6 AM Sunday: held-out gain is −0.02, the moderator is correctly discarded per D7, and the 70–82s beat is silently deleted — the word 'agentic' now appears nowhere demonstrable in an agent hackathon demo.
FIX: Add trace.json to the s6 contract today: every field the screen shows, written by the pipeline as it runs (proposal verbatim, result IDs, ΔLL numbers, decision, wall-clock timestamps), rendered raw-ish in the dashboard — machine-y is GOOD here, it proves it happened. Script the discard outcome as a first-class beat: 'the agent proposed X; held-out testing rejected it; a system that can say no to its own agent is the difference between discovery and hallucination' — that line is arguably STRONGER than +X%, but only if rehearsed, not improvised. Also resolve the human-approval-vs-overnight contradiction in D7/M5: solo hacker asleep cannot approve the remap; either approve before sleeping or move the remap to 7 AM.

[major] (Design §9 differentiation line vs §4.3 question config) The one-sentence differentiation ('AutoSynthesis runs meta-analysis on moderators you specify; we DISCOVER the moderators') is falsified by the project's own config file: initial_moderators lists species, dose, duration, timing, etc. — and the most probable headline moderator (topic C's vitamin-C dose threshold, the pre-planned 'money shot') is pre-specified there. The only genuinely discovered moderator is whatever one variable the single Pass-3 iteration adds — and if that's 'treatment timing', a domain-expert judge will call it obvious. The survivable framing ('MetaForest/metaCART require a hand-built dataset; we build the dataset from raw literature AND validate the moderators AND surface what remains unexplained') exists in the research notes but never made it into the demo script.
FAILURE: Q&A, one skeptical judge: 'Your headline split is dose. Dose is in your config. What did the system discover?' Presenter points to the agent iteration; judge: 'so it discovered one variable, which failed/was timing — a physiologist would name that in five seconds.' The differentiation vs AutoSynthesis/Elicit/Consensus — the entire top-3 case — evaporates in one exchange, live, in front of the other judges.
FIX: Rewrite the differentiation answer today as a rehearsed 3-part reply that does not lean on 'discovery' of the headline moderator: (1) no one hands us a dataset — we build the finding-level table from raw literature with per-row provenance (nearest tools stop at extraction); (2) we don't summarize disagreement, we decompose it — ranked, permutation-tested moderators INCLUDING the losers, plus residual contradictions no other tool surfaces; (3) the agent loop extends the schema beyond what we specified, gated by held-out testing. Say 'automatic moderator ranking and validation, plus agentic schema extension' — reserve the word 'discover' for the agent-proposed variable only, and only if it passes.

[major] (Design §8.6 baselines vs §2 pipeline and D9 dashboard sections) The validation plan promises 'Baselines shown: majority vote + one-shot LLM consensus summary vs our conditional structure' — the single most persuasive demo device available (side-by-side: what a consensus tool says vs the regime map). But no stage s0–s7 produces either baseline artifact, and D9's dashboard section list (question → disagreement → moderators → tree → evidence → contradictions → trace) has no slot for it. It is claimed output with no producer and no screen.
FAILURE: The baseline never gets built because nothing in the milestone table owns it; the 0–15s hook ('reviews collapse literature into a consensus') is asserted verbally with no visual proof of what the consensus alternative looks like. Or worse: at 9:30 AM Sunday someone fires a live LLM call to generate the consensus paragraph, unfrozen and unversioned, violating the cached-artifacts-only rule minutes before demo freeze.
FIX: Add baseline.json to the s7 export contract: (a) majority-vote line computed from findings.parquet (one line of pandas), (b) one-shot consensus summary generated ONCE tonight via the same Claude API path, prompt and response archived like any extraction. Give it a dashboard slot as the opening panel: 'Ask a consensus tool: <gray quote saying evidence is mixed>. Ask the multiverse: <regime map>.' Total cost ~30 minutes; it is the cheapest wow-per-minute item in the entire plan.

[minor] (Design §9 beat 55–70s (evidence cards) + §6 cut order) Two fragilities in the evidence beat. (1) The provenance click-through targets live paperclip.gxl.ai citation URLs — a network dependency at demo time that the 'cached artifacts only' rule doesn't cover. (2) The declared UI fallback (static Plotly HTML export) silently deletes the click-a-leaf interaction that beat 55–70s is built on, and no fallback narrative accounts for that.
FAILURE: 12:50 PM Sunday, venue wifi under 200 attendees: presenter clicks the citation link for the money claim, gets a spinner, and the 'every claim traceable to source lines' beat — the trust climax — ends on a timeout. Or the Saturday-night Streamlit fallback fires and Sunday's rehearsed 'click a leaf' line has nothing to click.
FIX: Make cached quote + section + line-numbers the primary evidence rendering (already in the row schema — just render it fully); demote the URL to a caption. Pre-capture screenshots of the citation page for the top 3 demo claims as offline backup. If the static-HTML fallback triggers, pre-plan the narrative swap: leaf 'click' becomes scrolling to a pre-expanded evidence panel for one chosen leaf — decide which leaf tonight, not on stage.

[minor] (Design §9 beat 15–30s + D9 Section 1) At an event whose Track B copy says 'run them across thousands of papers', the likely honest corpus number (~50 papers under rate-limit caps) undersells next to teams bragging 500+ mapped papers, and the dashboard has no screening-funnel visual to reframe small-N as rigor rather than weakness.
FAILURE: Judge mentally compares '50 papers' against the previous team's '1,200 papers mapped' and dings ambition — when the true story is 'searched 900 → deduped 400 → screened 120 primaries → extracted 240 findings', which is a strength (screening rigor) left invisible.
FIX: Emit funnel counts at each stage boundary (s1–s4 already produce the numbers as a side effect) into the s7 export; render a 4-bar funnel in Section 1 and speak the biggest number first: 'we searched N hundred papers and distilled them to M findings that actually test the question.'

