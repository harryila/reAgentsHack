VERDICT: The design is unusually self-aware — §41 modes 1, 3, 4, 6, 9, 10 and most of the UNVERIFIED list are genuinely covered with gates and fallbacks — but its gaps cluster in three patterns that will all detonate between 8 PM tonight and 8 AM tomorrow. (1) Missing failure branches: every gate assumes the interesting outcome exists — G2 has no floor if NO topic disagrees, G3 misdiagnoses genuine consensus as a prompt bug, the demo narrative has no script for a negative remap gain, and D7's human-approval requirement is flatly incompatible with M5's overnight schedule. (2) Enforcement gaps dressed as mitigations: conclusion-bias defense is a prompt plus a detector field produced by the suspect model; the 15–20-row audit is asked to certify an 85% precision claim (Wilson CI ~[64%,95%]) AND detect conclusion bias AND catch cohort duplicates — it has the statistical power for none of these, and the cross-model review that would fix it was in the research notes but dropped from the design. (3) One whole failure mode absent: publication bias (§41-7) appears nowhere, in front of a judge panel that includes the forking-paths author. The good news: every fix above is cheap NOW — protocol sentences, a grep pass at triage, a section-check on evidence_lines, a pre-registered cap rule, a re-sequenced evening — and all become expensive or impossible after topic lock at 5 PM. Recommended before approving the plan: fold findings 1, 2, 5, and 9 into the G2/G3 protocol text tonight, restore cross-model review as an overnight batch, and add the one-sentence publication-bias caveat to §8. With those patches this plan is executable; without them it is betting the demo on the happy path.

[blocker] (§5.2 Gate G2 / §5.3 Gate G3) There is no branch for 'no topic shows real disagreement.' G2 says 'choose the topic with best (disagreement × extractability × volume)' — an argmax always exists, so a topic gets locked even if every candidate scores badly. Worse, G3's failure branch ('otherwise fix prompts/schema first') hard-codes the diagnosis that low entropy means an extraction bug, when it can equally mean the literature genuinely agrees. The two causes have opposite remedies (fix prompts vs. pivot topic) and the design gives no way to tell them apart and no pivot path (topic D, an F-type expert suggestion, or a reframed 'consensus map' demo).
FAILURE: 5 PM: all three topics probe at entropy ~0.2; C gets locked anyway because it 'won.' 8 PM: G3 entropy still <0.4; the protocol sends you into prompt-fixing. You burn 9 PM–midnight re-prompting an extractor that was never broken, discover at the M4 pattern check (10:30 PM, per the milestone table) that there is no multiverse, and it is now too late to re-run triage on another topic. The entire thesis of the demo — 'measure where literature disagrees' — has nothing to point at.
FIX: Add floor thresholds to G2 (e.g., probe entropy ≥0.4 on ≥1 topic AND est. ≥40 usable primaries; else immediately escalate to C+B merge, topic D, or ask a domain expert at the venue for an F-type topic — decide this ladder now). At G3, use the anchor-paper check (already in §5.3) as the disambiguator: anchors extracted wrong → fix prompts; anchors right + entropy low → the literature agrees, pivot or reframe. Pre-decide whether a 'consensus + where it breaks by outcome family' demo is an acceptable fallback narrative.

[major] (D7 + M5 + §9 demo narrative) Three overnight contradictions with no owner or trigger. (1) D7 requires human approval before the re-map fires, but M5 schedules the agentic iteration 'overnight/early AM' — the approval gate and the sleep schedule are mutually exclusive. (2) Nothing watches the overnight scale run: the venue checkpoint ends 10:15 PM, M6 is 9 AM, and no mechanism detects a crashed extraction in between. (3) The demo narrative hard-codes 'held-out gain +X%' — there is no scripted version for the (likely!) case that the one budgeted remap iteration yields zero or negative held-out gain. Related unverified sentinel: research-notes UNVERIFIED #5 (does map silently drop zero-finding papers?) is NOT in the s0 checklist, and silent row-drop is exactly what makes an overnight partial failure invisible.
FAILURE: 11:30 PM: pass-3 proposals come back; you're asleep, so either the remap never fires (agent-loop trace section of the dashboard is empty — it's a fixed beat in the 90-s narrative and the 'Excellent' success criterion) or it fires unapproved. Separately, window 12/40 hits an uncaught rate-limit variant at 1 AM; the process exits; at 8 AM the corpus is stuck at ~30 papers with bootstrap/permutation runs never started, and papers that returned empty maps are indistinguishable from papers the run never reached.
FIX: Re-sequence: run pass 3 immediately after M4 (~10:30 PM), approve proposals before sleeping, queue the approved remap to run unattended. Add a watchdog: heartbeat log per window, auto-resume from last window (stages are already content-addressed — exploit it), and a phone notification on fatal exit. Add to s0: include one known-irrelevant paper and assert every input paper_id appears in map output (eligible=false or with findings). Pre-write the honest-negative demo line: 'the agent proposed X; the held-out test rejected it — the system doesn't believe its own hypotheses without evidence' (this is defensible and on-brand; deciding it at 2 AM is not).

[major] (§7 risk register + §8 credibility plan (missing item: context §41-7)) Publication bias is the ONE §41 failure mode with zero coverage anywhere in the design: no risk-register row, no credibility-plan item, no narrative caveat, no owner. The context doc's own mitigation is nearly free ('say this clearly'). This is not academic: the judge panel includes James Zou, whose forking-paths paper is literally about selective reporting, and the C money-shot ('aggregate null resolved by dose threshold') is precisely the kind of claim whose direction proportions are distorted by which nulls got published.
FAILURE: Sunday 1 PM, Q&A after the demo: 'Your entropy measures disagreement in the published, indexed literature — isn't that just publication-bias-filtered residue? How do your direction proportions relate to conducted experiments?' There is no prepared slide, caveat card, or sentence. A methods-savvy panel reads the omission as naivety, and it undercuts the whole 'credibility layer' positioning that is the project's differentiator.
FIX: Add a risk row (owner: dashboard build, trigger: M6 checklist). Add one caveat card to the dashboard evidence section ('proportions are of published findings; the indexed literature is not the universe of conducted experiments') and one rehearsed sentence for Q&A. If effect sizes turn out commensurate, a funnel-plot glance is a 15-minute stretch add. Total cost: <30 min; do not let this be the free point you dropped.

[major] (§5.3 / §8.1 / Gate G3) A 15–20-row audit cannot support the '≥85% precision' claim or reliably operate the G3 gate. Wilson 95% CI for 17/20 correct is [64%, 95%]; for 13/15 it's [62%, 96%]. As a gate, n=20 with a ≥17 pass bar has ~13% false-fail probability for a truly-90%-accurate extractor (wasted prompt-fixing cycle at 8 PM) and ~22% false-pass for a truly-75% one (you scale garbage overnight). Compounding this: §8.1 pre-scripts the audit's conclusion ('errors were omissions, matching published profiles') before any audit exists, and the design silently dropped the cross-model review that research-notes §3 recommended — the only cheap way to get a check across ALL rows rather than 20.
FAILURE: You demo '85% extraction precision (17/20 audited)' with two significant figures; Zou or any stats-literate judge mentally computes the binomial CI and the credibility layer — your differentiator — becomes the weak point. Or worse: a true-75% extractor passes G3 by luck, scales overnight, and Sunday morning's headline tree is built on rows where 1 in 4 is wrong, discoverable only when a judge clicks an evidence card whose quote contradicts the row. If audit errors turn out to be direction flips rather than omissions, the pre-scripted 'omissions' line is factually false on stage.
FIX: Report raw counts, never a percentage alone ('17/20 audited rows correct'), with the Wilson interval in a dashboard footnote — this reads as MORE credible to this panel, not less. Restore the cross-model check as an overnight batch job: a non-extractor model verifies evidence_quote ⇒ effect_direction for every row; report agreement rate over the full dataset (n in the hundreds fixes the CI problem). Make G3 a conjunction: human audit ≥17/20 AND anchor papers exactly match known results AND cross-model agreement ≥ threshold. Write the audit-conclusion sentence from the data, not in advance.

[major] (§5.2 triage scoring (missing: context §29 last bullet)) §29's final bad-topic criterion — 'the interesting contradiction disappears because search retrieval mixed unrelated questions' — has no counterpart in the triage scores (n_eligible, entropy, coverage, outcome concentration, review pollution). Triage entropy is computed over probe extractions, but the multi-family query strategy (deliberately including 'X increases Y' AND 'X decreases Y' phrasings) is optimized to retrieve papers about different relations: for C, 'antioxidant exercise' pulls both chronic training-adaptation studies (blunting = negative) and acute exercise-induced-oxidative-stress studies (attenuation = positive under a loose reading). Mixed questions = inflated entropy = the topic looks like a disagreement goldmine. The C+B merge is the deliberate version of the same mechanism: its global entropy is partly manufactured by pooling two questions.
FAILURE: C scores probe entropy 0.7 at triage and gets locked at 5 PM. At M4 the tree's first split is effectively 'which question was this paper answering' (comparison type / outcome family), which is a non-discovery; or the G3 human audit reveals half the probe findings never tested antioxidant+training vs placebo+training, and you re-triage at 9 PM with the evening gone. For C+B: a judge sees the global 'mixed' bar, then sees intervention_class trivially explains it, and concludes you engineered the disagreement.
FIX: Add a relation-purity score to triage: a human reads the 10 probe rows' intervention/comparator/outcome (~5 min/topic) and counts the fraction testing the pinned target relation; <70% → tighten eligibility before trusting the entropy number. Pin the comparison precisely in each config's eligibility criteria (for C: antioxidant+training vs placebo+training, chronic adaptation outcomes only). For the C+B variant, pre-commit the presentation order: show the intervention_class split FIRST as the trivial baseline, then the discovered moderators within class — that turns the manufactured pooling into an honest framing.

[major] (§7 conclusion-bias row / §4.1 author_conclusion_agrees / D2) The §41-2 mitigation is circular and its detection trigger is underpowered. The same model, reading the whole paper (abstract and discussion included), extracts both effect_direction and author_conclusion_agrees — a model biased toward the authors' spin will serenely report agreement. The only listed trigger is 'audit finds quote/direction mismatch,' i.e., a 15–20-row sample detecting a bias that may affect a modest fraction of rows. And the design's own acknowledged degradation path makes it worse: abstract-only papers (a known risk for C's paywalled exercise-phys journals) are extracted from 100% author-framed text. For C specifically, the dose-threshold narrative is famous folklore — an extractor echoing what papers SAY about Paulsen-vs-Yfanti would reproduce the narrative and fake-confirm the headline result.
FAILURE: The dose split shows up beautifully at M4 — because the extractor absorbed each paper's discussion-section framing of the dose story rather than the measured results. The recovery check 'passes' for the same reason (it recovers the narrative, not the data). Nobody can detect this because the detector field is produced by the suspect. A judge who pulls up one evidence card whose quote comes from a Discussion section, contradicted by the paper's own Table 2, unravels the provenance story live.
FIX: Add the enforcement mechanism the design lacks, and it's automatable: evidence_lines are line numbers and the VFS exposes sections/ — flag every row whose evidence_quote lines fall in Abstract/Discussion/Conclusion rather than Results/Methods, and report the flag rate as a dashboard data-quality stat (rows flagged get demoted from headline leaves). Make author_conclusion_agrees non-circular: a separate cheap pass extracts the author's claimed direction from the abstract ONLY, compared mechanically against the Results-derived direction. Fold both checks into the overnight cross-model batch from the audit fix.

[major] (§7 duplicate-cohorts row / §4.1 research_group) The §41-8 mitigation (DOI/PMID dedupe + NCT regex) misses the dominant duplication mode in exactly this corpus: companion papers from one trial. The design's own anchor papers are the counterexample — Yfanti 2010 and Yfanti 2011 are two publications from the SAME vitamin C+E training RCT, with different DOIs and PMIDs, from an era/field where trial registration IDs are frequently absent from the text, so the NCT regex fires on nothing. The one field that would catch this (research_group) is annotated 'later if time' — i.e., no owner, first thing cut. The C+B merge amplifies it: the metformin×exercise literature is explicitly noted as lab-concentrated, meaning few cohorts spawning many papers.
FAILURE: The headline high-dose leaf contains Paulsen 2014 + Yfanti 2010 + Yfanti 2011 counted as 3 independent papers; the min-support threshold (≥3 papers) is 'met' by 2 real cohorts; paper-level bootstrap treats the twins as independent, so the reported stability is inflated for the single most-scrutinized claim in the demo. An exercise-physiology-adjacent judge (this is a bio hackathon co-hosted by Arc/Biohub) recognizes the identical cohort descriptions in adjacent evidence cards and asks why your n counts them twice.
FIX: At ≤50 papers this is solvable by hand: during the G3 audit, run a same-cohort pass over every paper in a headline leaf (author-overlap + year within 2 + identical sample size/intervention ⇒ same cohort_id); populate dataset_or_cohort_id for the anchors right now (5 minutes — you already know Yfanti 2010/2011 are twins). Add a sensitivity rerun with cohorts collapsed and report 'the split survives cohort-collapsing' — which converts the vulnerability into a credibility point. Give the check an owner and a trigger: audit step, gate G3.

[major] (§7 rate-limit row + §8.3 recovery check + §9 narrative) The rate-limit fallback 'cap corpus at ~50 papers' specifies no sampling rule for WHICH 50, and demo claims are stated as facts about 'the literature.' Taking the top-N of a relevance-ranked search re-introduces §41-1 fame/positivity bias through the back door, after the design carefully mitigated it with multi-family queries. Downstream, two demo-facing artifacts inherit the bias: the global stacked bar (specific direction proportions in the 90-s narrative) and the recovery check against the published 2019 SR/MA null — which can FAIL on a biased 50-paper subsample even when the method is sound.
FAILURE: Rate limits bite at 11 PM; you cap at the first 50 papers, which skew toward highly-cited positive results. Sunday 7 AM, the recovery check contradicts the published pooled-null — with no time to diagnose whether your method or your sample is wrong, you either drop credibility item 3 (a hole in §8) or show a 'validation' that visibly fails. Alternatively the stacked bar says '55% positive' on stage when the balanced literature is null-heavy, and that number is now in the judges' notes as your claim.
FIX: Pre-register the cap rule in the config NOW: stratify the 50 across query families (guaranteeing the null/negative-phrasing families keep their share) and across publication years; log retained-vs-retrieved composition per family and show it in the dashboard's query-provenance panel (already planned — extend it one column). Phrase every global proportion as 'of our N-paper retrieved corpus,' never 'of the literature.' If the recovery check fails, the pre-planned diagnosis is: rerun it on the family-balanced subset before concluding anything.

[major] (§5.2 triage scores + D10 tie-break) The triage protocol cannot actually protect the C decision it is designed to make. Research notes are explicit: C's 60–120 corpus is 'inferred, not counted' and C drops behind A below ~60 usable primaries. But the design's n_eligible estimate comes from count queries × a review-pollution rate measured on a 10-paper probe — binomial noise of roughly ±30 percentage points — while the tie-break rule ('toward C/C+B') biases the decision in exactly the direction the noise permits. Separately, triage scores moderator-field COVERAGE but not level BALANCE: the C money-shot requires ≥3 papers on BOTH sides of the ~500 vs ~1000 mg dose threshold, with full text (dose lives in Methods, so abstract-only papers contribute nothing) — this joint requirement (primary × full-text × dose-reported × both regimes populated) is never directly counted anywhere.
FAILURE: Triage estimates 70 usable C primaries from a lucky probe; C locks at 5 PM. Overnight the real corpus tops out at ~40 primaries, half abstract-only; dose is populated for 18 findings, 15 of which sit in one regime. At M4 the dose moderator fails D6's min-support thresholds (3 papers / 8 findings per level), the tree can't make the advertised split, and there is no prepped alternative headline — the demo hook ('do vitamin pills cancel your workout?') survives but its answer doesn't.
FIX: During triage, measure the headline moderator directly instead of inferring it: Paperclip's corpus-wide trigram grep is fast — count papers matching dose patterns ('1000 mg', '500 mg', vitamin-dose regexes) per regime, and count probe papers-per-dose-bin. Require the headline moderator to clear projected min-support on BOTH sides before the tie-break toward C is allowed to fire. Pre-commit a fallback headline moderator for C (training status, vitamin identity, outcome family) so a dose failure at M4 costs a re-rank, not a story.

[minor] (§7 risk register structure) The register has no owner column and two rows with non-actionable triggers: 'Demo-time network flakiness' lists trigger '—', and several detection paths ('audit finds mismatch') route through the underpowered 20-row audit. With a solo hacker + agent, 'owner' really means 'which gate/checklist forces the look' — and publication bias, cohort dedupe, and the overnight watchdog currently have none. Additionally, G2's hard 5 PM topic-lock is silently auth-relative (triage needs live search, OAuth still pending at 2 PM) with no degradation rule.
FAILURE: Each orphaned risk fails silently until a gate that wasn't designed to catch it happens to; e.g., auth lands at 5:30 PM and there is no pre-decided compressed-triage variant (fewer topics? counts+anchors only?), so the decision gets improvised exactly when tired.
FIX: One 10-minute pass: attach every register row and every fix from this audit to a named gate or milestone checklist (G1/G2/G3/M4/M6), and add a time-boxed auth degradation rule: auth by 4 PM → full triage; by 6 PM → triage C and C+B only; later → lock on count queries + anchor lookups + pre-scan verdict, and say so honestly in the triage table.

